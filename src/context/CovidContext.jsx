// import create context

import { createContext } from "react";

// create context and set initial value
const CovidContext = createContext(null);

// export movies context
export default CovidContext;
