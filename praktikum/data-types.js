const name = "Ilham Arifin"; // string
const age = 20; // number
const isMarried = false; // false
let dateAt; // undifined

/**
 * operator typeof untuk cek tipe data
 */
console.log(typeof name, typeof age, typeof isMarried, typeof dateAt);
